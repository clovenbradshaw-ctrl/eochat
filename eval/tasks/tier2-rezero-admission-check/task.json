{
  "id": "tier2-rezero-admission-check",
  "tier": 2,
  "title": "Bug fix from symptom only: re-zero trigger admits the wrong shape",
  "sourceNote": "Grounded in eoreader6 commit 5734621 (packages/engine/emergence/paradigm.js), reduced to a small self-contained admission-check function for this harness. The model is given only the buggy code and a symptom report, never the real fix or its diff.",
  "system": "You are a careful software engineer fixing a real, reported bug. Output ONLY the complete, corrected file content for solution.js — no markdown fences, no commentary, no explanation.",
  "maxTokens": 350,
  "seed": 101,
  "targetFile": "solution.js",
  "fixtureFiles": ["nul-helpers.mjs"],
  "prompt": "Fix the bug described below. Return the COMPLETE corrected content of solution.js (it must still export a function named exactly `admitsRezeroTrigger` that imports whatever it needs from \"./nul-helpers.mjs\").\n\nCURRENT FILE (solution.js) — HAS A BUG:\n```js\nimport { isGap } from \"./nul-helpers.mjs\";\n\n// Whether `prior` licenses a re-zero (a reset that concedes the old paradigm\n// and begins a new one). This must be true ONLY when `prior` is the real,\n// measured \"paradigm unraveled\" trigger — never a default, never a guess.\nexport function admitsRezeroTrigger(prior) {\n  if (prior === undefined || prior === null) return false;\n  const unraveled = isGap(prior) ? prior : prior.unraveled ?? null;\n  const priorLabels = Array.isArray(prior?.paradigm) ? prior.paradigm : Array.isArray(unraveled?.paradigm) ? unraveled.paradigm : null;\n  if (unraveled?.gap !== \"paradigm_unraveled\" && !priorLabels) return false;\n  return true;\n}\n```\n\nBUG REPORT (symptom only — no root-cause diagnosis given):\nA caller passes the ROUTINE, non-unraveled result of an unrelated upstream check into `admitsRezeroTrigger`. That result happens to be shaped like `{ refused: false, paradigm: [\"kindA\", \"kindB\"] }` — it carries a `paradigm` array, but it is NOT the real unravel trigger; nothing was ever measured as unraveled. `admitsRezeroTrigger` incorrectly returns `true` for this input, causing a downstream reset to fire on completely ordinary, non-unraveled input. This is a real, observed defect, not a hypothetical.\n\nREQUIREMENT: `admitsRezeroTrigger(prior)` must return `true` if and only if `prior` is the actual measured trigger — that is, `isGap(prior) === true` AND `prior.gap === \"paradigm_unraveled\"` — and `false` for every other input, including: the shape from the bug report above, a gap of any OTHER type, an object that merely carries `unraveled` or `paradigm` fields without being a real gap, `null`, `undefined`, and a plain empty object.\n\nFix the function. Return the full file."
}
